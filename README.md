# DNS Check — Веб-инструмент для диагностики DNS-записей

> **🌐 Сервис доступен по адресу:** [https://izelenov7.github.io/dig-web](https://izelenov7.github.io/dig-web)

Профессиональный веб-сервис для проверки DNS-записей доменов с минималистичным дизайном.

## 🚀 Возможности

### Основные функции
- **Проверка DNS-записей** — 7 типов записей (A, AAAA, MX, NS, TXT, CNAME, CAA)
- **Множественные DNS-резолверы** — Cloudflare, Google, OpenDNS, Яндекс, NextDNS, Mullvad
- **Нерекурсивные запросы** — пошаговое отображение делегирования
- **IDN поддержка** — работа с интернационализированными доменами (.рф и др.)

### Опции запроса
| Опция | Описание |
|-------|----------|
| **Команда** | Отображает выполняемую dig команду |
| **Без рекурсии** | Нерекурсивный запрос с пошаговым отображением |

### Форматы вывода
- **BIND** — чистые записи в формате: `домен TTL IN TYPE данные`
- **Dig** — полный вывод с заголовками и секциями
- **NoRec** — нерекурсивный запрос

## 🛠 Технологический стек

- **Frontend**: React 18 + TypeScript
- **Сборка**: Vite
- **Стили**: Tailwind CSS v4
- **State Management**: Zustand
- **DNS API**: DNS-over-HTTPS (Cloudflare, Google)

## 📦 Установка

```bash
# Клонирование репозитория
git clone https://github.com/izelenov7/dig-web.git
cd dig-web

# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка для продакшена
npm run build
```

## 📖 Использование

### Базовый запрос
1. Введите доменное имя (например, `google.com`)
2. Выберите тип записи (A, MX, NS и т.д.)
3. Выберите DNS-резолвер (Cloudflare, Google и др.)
4. Нажмите «Выполнить запрос»

### Выбор DNS-резолвера
В секции «DNS-резолверы» выберите:
- **Default** — резолвер по умолчанию
- **Preset** — предустановленные (Cloudflare, Google, OpenDNS, Яндекс, NextDNS, Mullvad)
- **Authoritative** — авторитативные серверы домена
- **Custom** — собственные серверы

### Дополнительные опции
Нажмите на чекбоксы справа от кнопки «Выполнить запрос»:
- **Команда** — показать dig команду
- **Без рекурсии** — нерекурсивный запрос

### Копирование результатов
- **Копировать все** — копирует все записи из секции «Ответы»
- **Копировать (BIND/Dig/NoRec)** — копирует выбранный формат
- **Сохранить в файл** — скачивает файл с записями в формате BIND

## 📁 Структура проекта

```
src/
├── components/
│   ├── dns/           # DNS-компоненты
│   │   ├── Header.tsx
│   │   ├── DomainInput.tsx
│   │   ├── RecordTypeSelector.tsx
│   │   ├── NameserverSelector.tsx
│   │   ├── QueryOptionsBar.tsx
│   │   ├── ResultsPanel.tsx
│   │   └── SubmitButton.tsx
│   ├── ui/            # Базовые UI-компоненты
│   └── Documentation.tsx
├── hooks/             # React-хуки
│   └── useDnsQuery.ts
├── lib/               # Утилиты и сервисы
│   ├── dnsService.ts  # DNS-over-HTTPS сервис
│   ├── idn.ts         # IDN/Punycode конвертация
│   ├── tldNameservers.ts # TLD серверы
│   └── utils.ts       # Общие утилиты
├── store/             # Zustand store
│   └── dnsStore.ts
├── types/             # TypeScript типы
│   ├── dns.ts         # Типы DNS-записей
│   ├── nameservers.ts # Типы резолверов
│   └── options.ts     # Типы опций
└── App.tsx            # Главный компонент
```

## 🔌 API

### DNS-over-HTTPS эндпоинты

| Провайдер | URL |
|-----------|-----|
| Cloudflare | `https://cloudflare-dns.com/dns-query` |
| Google | `https://dns.google.com/resolve` |

### Поддерживаемые типы записей

| Тип | Описание | RFC |
|-----|----------|-----|
| **A** | IPv4 адрес хоста | RFC 1035 |
| **AAAA** | IPv6 адрес хоста | RFC 3596 |
| **CNAME** | Каноническое имя (псевдоним) | RFC 1035 |
| **MX** | Почтовый обменник для приёма email | RFC 1035 |
| **NS** | Авторитативные DNS-серверы домена | RFC 1035 |
| **TXT** | Текстовая запись для SPF, DKIM, DMARC | RFC 1035 |
| **CAA** | Разрешённые центры выдачи сертификатов | RFC 8659 |

## 🌐 Поддержка IDN доменов

Сервис автоматически конвертирует интернационализированные домены:
- **Ввод**: `пример.рф`
- **DNS запрос**: `xn--e1afmkfd.xn--p1ai`
- **Отображение**: `пример.рф` (оригинальный вид)

### Поддерживаемые зоны
- `.ru` — Россия
- `.su` — Советский Союз
- `.рф` (xn--p1ai) — Российская Федерация (кириллица)

## 📝 Форматы вывода

### BIND формат
```
google.com.		300	IN	A	142.250.50.46
```

### Dig формат
```
; <<>> DiG 9.18.0 <<>> A google.com
;; QUESTION SECTION:
;google.com.			IN	A

;; ANSWER SECTION:
google.com.		300	IN	A	142.250.50.46

;; Query time: 42 msec
;; SERVER: Cloudflare (1.1.1.1)
```

### NoRec формат (нерекурсивный запрос)
```
;; Step 1: Root Servers
;; Querying root servers for TLD nameservers...

;; Step 2: TLD Servers for .com
;; Querying .com TLD nameservers...

;; Step 3: Authoritative Nameservers
;; Querying authoritative nameservers for google.com...

;; Step 4: Final Query
google.com.	300	IN	A	142.250.50.46
```

## 🔧 Конфигурация

### Переменные окружения
Не требуются. Все запросы выполняются через публичные API.

### Настройка Tailwind CSS
Конфигурация находится в `src/index.css`:
```css
@import "tailwindcss";

@theme {
  --color-primary-50: #eff6ff;
  --color-primary-100: #dbeafe;
  /* ... другие цвета */
}
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменений (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License — см. файл LICENSE для деталей.

## 📞 Контакты

Для вопросов и предложений:
- Telegram: [@igorqx](https://t.me/igorqx)
- VK: [igorqx](https://vk.com/igorqx)
- GitHub Issues: [github.com/izelenov7/dig-web/issues](https://github.com/izelenov7/dig-web/issues)
